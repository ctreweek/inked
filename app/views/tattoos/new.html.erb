<div class="row">
  <div class="col-lg-4 col-lg-offset-4">
    <%= simple_form_for [current_artist, @tattoo] do |f| %>
    <%= f.input :photo, as: :attachinary %>
    <%= f.button :submit, "Upload" %>
    <% end %>
  </div>
</div>
