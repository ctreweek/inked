<% @conversations.each do |conversation| %>
  <% if current_user && conversation.user.id == current_user.id %>
    <%= link_to conversation.artist.name, conversation_messages_path(conversation) %>
  <% end %>
<% end %>
